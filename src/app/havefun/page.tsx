import { Header } from "../(components)/Header";

export default function HaveFun() {
  return (
    <main className="w-full flex flex-col items-center gap-4">
      <Header title="Have fun" subtitle="REPLACE ME" />
    </main>
  );
}
