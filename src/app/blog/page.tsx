import { Header } from "../(components)/Header";

export default function Blog() {
  return (
    <main className="w-full flex flex-col items-center gap-4">
      <Header title="Blog" subtitle="REPLACE ME" />
    </main>
  );
}
